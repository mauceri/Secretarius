# Dépendances pour la génération de corpus synthétique (corpus/)
# Environnement séparé de l'environnement d'entraînement (llenv).
#
# Installation :
#   python -m venv corpus_env
#   corpus_env/bin/pip install -r requirements-corpus.txt
#
# Clés API requises (selon le fournisseur) :
#   export DEEPSEEK_API_KEY=sk-...   # pour DeepSeek (recommandé)
#   export OPENAI_API_KEY=sk-...     # pour OpenAI

dspy-ai>=2.5
gepa>=0.0.17
litellm>=1.30
openai>=1.0
tqdm
python-dotenv
